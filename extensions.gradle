allprojects {
    ext {
        mppJavaAndAndroidProjectPresetPath = "${rootProject.projectDir.absolutePath}/mppJavaAndAndroidProject.gradle"
        defaultAndroidSettingsPresetPath = "${rootProject.projectDir.absolutePath}/defaultAndroidSettings.gradle"
    }
}
